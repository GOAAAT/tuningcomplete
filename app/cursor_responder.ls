module.exports = class CursorResponder
  select-at: (pt) ->
  scale-by: (sf, pt) ->

  pointer-down: (pt) ->
  pointer-up: (pt) ->
  pointer-moved: (pt) ->

  pan-by: (delta) ->

  pointers-changed: (pts) ->
